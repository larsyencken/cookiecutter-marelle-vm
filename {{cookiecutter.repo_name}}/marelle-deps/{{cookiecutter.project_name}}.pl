%
%  {{project_name}}.pl
%  {{repo_name}}
%
%  Dependencies for setting up {{project_name}}.
%

pkg('{{project_name}}').
met('{{project_name}}', _) :- fail.
meet('{{project_name}}', _) :-
    throw(fill_me_in).
